# TODO

最后更新：2026-08-13

## 工作区与发布状态

- 已发布基线：`main` 已包含 PR #1（vSwap/eSpace 测试网）、PR #2（接手文档）、PR #3
  （钱包切网、生命周期折叠、质押/解质押 MAX）和本地数据导入导出。2026-08-13 已核验
  生产首页包含“本地数据”入口，eSpace 生产网络为 Mainnet 1030。
- 最新已发布 `main` SHA 为 `03fff72c7e63e76554d96e1a360d9e56e4b09ae2`；对应
  `deploy-pages.yml` 于 2026-08-05 成功，生产站点可访问。
- 当前功能分支：vSwap 仓位详情已完成，并通过 113 项单测、8 项 E2E、类型、Lint、格式
  和生产构建；尚未合入 `main` 或部署。
- 生产状态：线上版本尚不包含 vSwap 仓位详情。只有在用户明确授权发布、Pages 成功并
  完成生产核验后，才能将该功能标记为已发布。
- eSpace 主网和测试网真实仓位读取已验证；375px 无横向溢出且浏览器控制台无错误。
- Core 测试网真实 Fluent 写交易闭环已完成。

## 当前检查点

- [x] Core 余额、标准 PoS 多池查询、聚合与失败隔离。
- [x] 地址总览展示 Core 总资产分项，失败池以已读取资产下限提示。
- [x] 地址总览展示预计下次可质押时间：以可用余额、未领取收益、全部有效质押及链上
      APY 加权估算达到一票和基础费用预留的时间；数据或 APY 不完整时明确降级。
- [x] 地址总览展示预计每日收益：按全部有效质押和对应池链上 APY 逐池加权并折算一天
      的预计 CFX 收益；数据或有效持仓池 APY 不完整时不展示部分估算。
- [x] 标准 PoS Pool 合约近 7 天预期 APY 展示，旧池不支持时降级且不影响持仓。
- [x] 首页池列表展示预期 APY、池总质押，支持 APY、总质押和收藏顺序排序。
- [x] 地址资产明细池列表支持 APY、有效质押和未领取收益排序。
- [x] 首页与地址明细的池排序偏好按 Core 网络缓存并在后续访问时自动恢复。
- [x] 地址/池收藏、池 ABI 校验、地址快捷切换。
- [x] 当前 Core 网络的收藏地址、PoS 池和排序偏好支持 JSON 导入导出，导入时校验网络并合并。
- [x] 池详情及四类 Core 交易 UI 与 Fluent 安全门禁。
- [x] 地址总览一键领取：按当前 Core 网络本地最低收益门槛筛选成功读取的池，逐笔经
      Fluent 确认、动态费用校验和回执后领取；失败或拒绝时停止后续未发送交易。
- [x] Fluent 已连接但 Core 网络不匹配时可由操作区直接请求切换到当前配置网络。
- [x] 质押操作显示当前余额并支持整票 MAX，解质押支持按治理锁定扣除后的票数填入 MAX。
- [x] 池详情展示增加质押锁定、可解质押、解质押等待和本金提取生命周期时间线。
- [x] 质押生命周期默认收起并支持展开/收起，减少对钱包操作区域的遮挡。
- [x] 增加质押锁定只展示目标区块尚未到达的 `inQueue` 节点。
- [x] eSpace 主网原生 CFX 余额查询与范围说明。
- [x] eSpace 主网 vSwap LP Farming 仓位、token 数量、手续费和奖励只读展示。
- [x] eSpace 测试网本地模式、网络标签、独立 Query key 与 vSwap 测试网配置。
- [x] 地址列表保持收藏顺序，未收藏当前地址追加到末尾。
- [x] 地址列表仅高亮当前项，并显示 Core/eSpace 规则对应的总 CFX。
- [x] Lucide 图标切换跟随系统、明亮、暗黑主题及偏好持久化。
- [x] Core/eSpace 路由切换保持稳定滚动条槽位，页面不横向抖动。
- [x] Core 测试网仅本地调试模式启用，生产构建强制使用主网，主/测试网收藏数据隔离。
- [x] 测试网真实增加质押、交易回执与状态回读。
- [x] 解质押门禁排除 `inQueue`，签名前动态校验 gas 与存储抵押所需余额。
- [x] vSwap 整仓位失败和可选数据 warning 均触发 `≥` 下限提示。

## 已完成：Core 测试网写交易闭环

无敏感检查点：专用测试钱包在官方示例 `test6` 池有两笔各 1 票的 `inQueue`，锁定完成
区块分别为 `355878000`、`355879481`。钱包地址、私钥和交易哈希不入库；接续时由用户
连接同一测试钱包。若测试网重置或池状态变化，先重新建立测试状态。

预计时间基准：2026-07-02 10:48（Asia/Shanghai）测试网区块为 `353758532`，按约
2 区块/秒估算，第一票约在 2026-07-14 17:10、第二票约在 2026-07-14 17:23 可发起
解质押。时间只用于安排测试，必须以目标区块和实时链上状态为准。

- [x] 使用 SDK 直发 `claimAllInterest()` 验证测试网领取收益：回执成功、待领取收益归零、
      累计已领取增加且 2 票持仓不变；本功能已完成，不再安排重复测试。
- [x] 对账领取收益与池 APY：质押至领取期间池级 7 日毛 APY 约为 1699%，用户分成比例
      为 90%；实际领取为扣除池分成后粗估值的 98.4%，约 1.6% 偏差符合 7 日快照与约
      18 小时离散发奖周期的预期差异。
- [x] 使用真实 Fluent 验证已连接但网络不匹配时的“切换网络”确认、拒绝和成功状态。
- [x] 原有两笔 `inQueue` 已完成锁定后，以 `pnpm dev:testnet` 连接 Fluent，确认解质押
      MAX 为 2 票；当前测试网状态已无法复现“一票已解锁、另一票仍在 `inQueue`”的旧检查点。
- [x] 提交第一笔 1 票解质押，验证 Fluent 确认、成功回执、余额/持仓 Query 刷新和首个
      `outQueue`。
- [x] 提交第二笔 1 票解质押，验证两个独立 `outQueue` 和局部状态刷新。
- [x] 每笔解质押成功后约再等待 176400 区块（按 2 区块/秒约 24.5 小时），记录实际
      `outQueue` 解锁区块：第一笔为 `359641291`，第二笔为 `359641430`；本项必须以
      实时链上状态为准并在到达后验证可分批提取。
- [x] 两条 `outQueue` 到达目标区块后，本金转为可提取；实际以一笔“提取全部已解锁本金”
      交易提取 2 票，并验证回执成功、可提取本金与解质押中归零、钱包余额刷新。
- [x] 完成真实 UI 流程后运行完整验收：91 项单测、6 项 E2E、类型、Lint、格式和生产构建
      全部通过；交易模块已标记为写交易验证完成。

## 已完成（当前功能分支，尚未发布）：vSwap 仓位详情

- [x] 增加独立 vSwap NFT 仓位详情路由，并限制为当前地址经 subgraph 发现的 managed NFT。
- [x] 展示完整 Tick/价格区间、基于 `slot0.sqrtPriceX96` 的当前价格和双向报价切换。
- [x] 基于活动 incentive 展示预计每日 farming 奖励；继续使用 bigint/十进制字符串，
      不引入浮点金额计算。
- [x] 使用真实 eSpace 主网和测试网 managed NFT 样本核验详情读取、双向价格、活动
      incentive、每日奖励、375px 布局及浏览器控制台。

## 后续任务

- [ ] 正式 PR 合入 `main` 后核验 Pages 工作流与生产详情路由。
- [ ] 由用户确定 vSwap 详情发布后的下一项产品功能。

## 接手注意

- 不要把临时查询地址、钱包地址、测试账户或真实资产信息写入源码和文档。
- 不要新增内置池或放宽 Core 写交易门禁。
- 不要让导入导出绕过 `infrastructure/storage/localState.ts` 的 schema、Core 网络校验和
  合并规则；不得导入链上查询缓存或钱包敏感信息。
- 不要把 eSpace 测试网配置带入生产构建；网络选择必须继续由 `resolveESpaceNetwork`
  强制生产主网。
